# Formula

`Formula/heat.rb` is generated by the private Heat release pipeline when a new
binary release is published.

Do not hand-edit the formula unless the private release workflow is unavailable.
The formula should always point at this repository's public GitHub Release
assets and use the checksums from `SHA256SUMS`.
